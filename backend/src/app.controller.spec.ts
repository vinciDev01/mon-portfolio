// The default AppController has been removed.
// Feature modules have their own controllers and tests.
describe('AppModule', () => {
  it('should be defined', () => {
    expect(true).toBe(true);
  });
});
